# uploaded reference material (not part of the deployed deck)
Source Material/

# build output (regenerated by build.js / CI)
dist/

# reviewer exports staged for merge (merge into review/comments.json, don't commit raw)
review/inbox/*.json

# os / editor cruft
.DS_Store
Thumbs.db
node_modules/
*.log
